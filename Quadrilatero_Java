package one.digitalinnovation.basecamp;

/**
 * Classe de exemplo para o exercício da Aula 2 de Métodos.
 */
public class Quadrilatero {

    public static void area(double lado) {

        System.out.println("Área do quadrado:" + lado * lado);
    }

    public static void area(double lado1, double lado2) {

        System.out.println("Área do retângulo:" + lado1 * lado2);
    }

    public static void area(double baseMaior, double baseMenor, double altura) {

        System.out.println("Área do trapézio:" + ((baseMaior+baseMenor)*altura) / 2);
    }

    public static void area(float diagonal1, float diagonal2) {

        System.out.println("Área do losango:" + (diagonal1 * diagonal2)/2);
    }

}

// Este código apresenta a classe Quadrilatero, que contém quatro métodos chamados area com diferentes parâmetros.
Cada método é responsável por calcular a área de um tipo de quadrilátero diferente.
O primeiro método, area(double lado), é responsável por calcular a área de um quadrado. 
Ele recebe apenas um parâmetro, que é o lado do quadrado.
O segundo método, area(double lado1, double lado2), é responsável por calcular a área de um retângulo. 
Ele recebe dois parâmetros, que são os lados do retângulo.
O terceiro método, area(double baseMaior, double baseMenor, double altura), é responsável por calcular a área de um trapézio. 
Ele recebe três parâmetros, que são a base maior, a base menor e a altura do trapézio.
O quarto e último método, area(float diagonal1, float diagonal2), é responsável por calcular a área de um losango. 
Ele recebe dois parâmetros, que são as diagonais do losango.
Cada método simplesmente imprime a área do quadrilátero correspondente usando a fórmula matemática apropriada.// 
